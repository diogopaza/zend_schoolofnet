<?php

$this->headTitle("BLOG");

?>

<h1>Blog</h1>

<table class="table">
    <tr>
        <th>Title</th>
        <th>Action</th>
    </tr>
    <?php foreach($posts as $post): ?>
    <tr>
        <td><?=$this->escapeHtml($post['title']) ?></td>
        <td><?=$this->escapeHtml($post['content']) ?></td>
    </tr>
    <?php endforeach; ?>
</table>